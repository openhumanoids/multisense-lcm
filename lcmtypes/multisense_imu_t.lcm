package multisense;

// DEPRECIATED DEPRECIATED
// This message was used previously but duplicates
// bot_core_ins_t. So we not reuse the ins_t type


struct imu_t
{
  int64_t utime;
  double gyro[3];    // rotational velocity in rad/sec
  double accel[3];   // linear acceleration in m/sec^2
  double mag[3];     // magnetometer in gauss
}
